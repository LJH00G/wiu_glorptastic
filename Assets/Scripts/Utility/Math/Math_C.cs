
using UnityEngine;

namespace Utility.Math
{
    public static class Math_C
    {
        public const float F_EPSILON = 0.00001f;
        public const float F_EPSILON_3D = 0.001f;


        public const float PI = Mathf.PI;
        public const float PI_TWO = Mathf.PI * 2f;
        public const float PI_HALF = Mathf.PI * 0.5f;
        public const float PI_INV = 1f / Mathf.PI;
        public const float PI_SQR = Mathf.PI * Mathf.PI;
        public const float PI_SQRT = 1.772454f;
    }
}